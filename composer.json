{
  "name": "wptechnix/wp-plugin-boilerplate-tools",
  "description": "Development tooling for the WPTechnix WordPress Plugin Boilerplate.",
  "type": "project",
  "license": "GPL-2.0-or-later",
  "autoload": {
    "psr-4": {
      "WPPluginBoilerplate\\": "plugin/src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "WPPluginBoilerplate\\Tests\\": "tests/"
    }
  },
  "require-dev": {
    "brain/monkey": "^2.7",
    "mockery/mockery": "^1.6",
    "php-stubs/woocommerce-stubs": "^10.9",
    "phpstan/phpstan": "^2.1",
    "phpstan/phpstan-strict-rules": "^2.0",
    "phpunit/phpunit": "^9.6",
    "szepeviktor/phpstan-wordpress": "^2.0",
    "wptechnix/php-scoper-wordpress-excludes": "^1.0",
    "wptechnix/wp-coding-standards": "^1.1.2"
  },
  "config": {
    "allow-plugins": {
      "dealerdirect/phpcodesniffer-composer-installer": true
    },
    "platform": {
      "php": "8.0"
    },
    "sort-packages": true
  },
  "scripts": {
    "phpcs": "phpcs",
    "phpcbf": "phpcbf || true",
    "phpstan": "phpstan analyse --memory-limit=2048M",
    "lint": [
      "@phpcbf",
      "@phpcs",
      "@lint:compat",
      "@phpstan"
    ],
    "test": "phpunit",
    "lint:compat": "phpcs plugin/wp-plugin-boilerplate.php plugin/src/Requirements.php --standard=PHPCompatibility --runtime-set testVersion 5.6- --report=full"
  },
  "scripts-descriptions": {
    "phpcs": "Checks code against WordPress coding standards.",
    "phpcbf": "Automatically fixes coding standard issues.",
    "phpstan": "Runs static analysis on the codebase.",
    "lint": "Runs full code quality checks (PHPCBF, PHPCS, PHPStan).",
    "test": "Runs the unit test suite (no WordPress; Brain Monkey).",
    "lint:compat": "Verifies the pre-flight gate and the main plugin file still parse on PHP 5.6."
  }
}
